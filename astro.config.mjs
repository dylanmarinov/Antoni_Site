// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  // Uncomment and update these two lines before deploying to GitHub Pages.
  // Replace YOUR_GITHUB_USERNAME with your GitHub username.
  // Replace YOUR_REPO_NAME with what you name your GitHub repository.
  //
  // site: 'https://YOUR_GITHUB_USERNAME.github.io',
  // base: '/YOUR_REPO_NAME',
});
